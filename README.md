# Texture macOS Laptop Setup

This repository provides the standard Texture baseline setup for new macOS laptops.

## Usage

```bash
bash <(curl -fsSL https://texturehq.github.io/laptop-setup/bootstrap.sh)
```
